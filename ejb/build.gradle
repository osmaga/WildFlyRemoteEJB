apply plugin: 'war'

repositories {
    maven { url "http://repo.maven.apache.org/maven2" }
    mavenLocal()
}

dependencies {
	providedCompile project(':ejb-client')
	compileOnly group: 'javax', name: 'javaee-api', version:'7.0'
}